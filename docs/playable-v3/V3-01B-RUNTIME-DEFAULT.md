# V3-01B — Canonical runtime default

Playable V3 uses the premium roach identity by default. Legacy rendering is diagnostic-only and requires explicit `legacyVisual: true` opt-in.

World scale and collider tuning remain V3-02/V3-03 after visual runtime inspection.
