export const CELLAR_SET_DRESSING = Object.freeze({
  ownsPhysics: false,
  canOccludeCriticalTraversal: false,
  mutatesPlayerScale: false,
  mutatesCamera: false,
  maxPropAlpha: 0.76,
  maxMarkAlpha: 0.28,
} as const);
