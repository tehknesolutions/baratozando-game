export const PLAYER_VISUAL_SEPARATION = Object.freeze({
  actorOwnsPremiumTexture: false,
  actorOwnsPremiumScale: false,
  actorHasPhysicsBody: true,
  visualOwnsPremiumTexture: true,
  visualOwnsPremiumScale: true,
  visualHasPhysicsBody: false,
} as const);
